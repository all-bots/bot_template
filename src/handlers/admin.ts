import { Telegraf } from "telegraf";

export default async function registerAdmin(bot: Telegraf) {
  // bot.command('/total_user_follow_db', getTotalUserFollow);
  // bot.command('/total_follow', totalFollow);
  // bot.command('/total_user_follow', totalUserFollow);
  // bot.command('/total_user', totalUser);
  // bot.command('/total_withdraw', totalWithdraw);
  // bot.command('/total_error', totalError);
  // bot.command('/total_error_2', totalError2);
}
